# bspwm hotkeys

super + alt + Escape
    pkill -x yabar; bspc quit

super + w
    bspc node -c

super + {s,t,f}
    bspc node -t {floating,tiled,fullscreen}

super + m
    bspc node -s biggest

super + {_,shift + }c
    bspc node -f {next,prev}

super + bracket{left,right}
    bspc desktop -f {prev,next}

super + {h,j,k,l}
    bspc node -f {west,south,north,east}

super + shift + {1-5};
    bspc node -d {1-5};

~button1
    bspc node pointed -f

# make sxhkd reload its configuration files

super + alt + r
    pkill -USR1 -x sxhkd

# other

super + Return
    urxvt

super + space
    rofi -font "Roboto 11" -show run

alt + Tab
    rofi -font "Roboto 11" -show window
