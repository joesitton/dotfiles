#!/bin/sh

source /home/joe/.config/global.conf

dmenu_run \
    -i -r -l $L \
    -p "$PROMPT" \
    -x $X -y $Y \
    -w $W -h $H \
    -fn $FONT2 \
    -dim $DIM \
    -nb $BAR_BACKGROUND \
    -nf $BAR_FOREGROUND \
    -sb $MAIN_COLOR \
    -sf $BAR_FOREGROUND \
